// fx that accepts an array of positive integers and a positive integer
// should return the minimal length of a contiguous subarray which the sum is >= to integer passed to function
// if there isn't one, return 0 instead

